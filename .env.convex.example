# Convex/Auth deployment env
AUTH_SECRET=
SITE_URL=
JWT_PRIVATE_KEY=
JWKS=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
AUTH_RESEND_KEY=
PUSH_VAPID_PUBLIC_KEY=
PUSH_VAPID_PRIVATE_KEY=
PUSH_VAPID_SUBJECT=mailto:hello@dividir.app

# Optional: set only if auth callbacks should use a custom public domain
# instead of the default CONVEX_SITE_URL domain from the deployment.
# Omit this variable entirely when unused. Do not set it to an empty string.
CUSTOM_AUTH_SITE_URL=
